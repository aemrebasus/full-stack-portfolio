import { ToRouterLinkPipe } from './to-router-link.pipe';

describe('ToRouterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ToRouterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
